[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback so client-side routing / direct loads resolve to the app shell.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
